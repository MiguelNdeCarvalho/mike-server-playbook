#cloud-config

# Set machine Hostname
hostname: mike-pi

# Add my user
users:
  - name: mike
    gecos: MiguelNdeCarvalho
    groups: adm, cdrom, sudo, dip, plugdev, lpadmin, sambashare
    lock_passwd: false
    passwd: #change
    primary_group: mike
    shell: /bin/bash
    ssh_import_id:
      - gh:MiguelNdeCarvalho
    sudo: ALL=(ALL) ALL

# Disable password authentication with the SSH daemon
ssh_pwauth: false

## Update apt database and upgrade packages
package_update: true
package_upgrade: true
